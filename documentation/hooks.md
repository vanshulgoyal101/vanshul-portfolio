# Custom Hooks Documentation

## useSmoothScroll.js
- Provides smooth scrolling behavior for navigation and anchor links.
- Usage: Import and use in components that require animated scroll.
- Implementation: Uses `useEffect` and event listeners to override default scroll behavior.

## Extending
Add new hooks to `src/hooks/` and document their API and usage here.
